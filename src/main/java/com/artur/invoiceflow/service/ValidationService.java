package com.artur.invoiceflow.service;

import com.artur.invoiceflow.dto.validation.ValidationResultResponse;

public interface ValidationService {
//    ValidationResultResponse validateInvoice(Long invoiceId); // executa regras/score e atualiza status da invoice
//    ValidationResultResponse getByInvoiceId(Long invoiceId);  // última validação
}
